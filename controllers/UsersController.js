const Users = require('../models/Users')

exports.getUsersByPhone = async (req, res) => {
    // const filter = req.params.phone
    // try {
    //     const users = await Users.find({ phone: { $regex: '.*' + filter + '.*' } }).limit(5);

    //     if (users) {
    //         res.status(201).json(users)
    //     } else {
    //         res.status(500).send('not match')
    //     }
    // } catch (error) {
    //     res.status(500).send(error)
    // }
}